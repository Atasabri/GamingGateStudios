@model IEnumerable<GamingGateStudios.Models.Splash>

@{
    ViewBag.Title = "Splashes";
    Layout = "~/Areas/Operators/Views/Shared/_Layout.cshtml";
}

<div style="width:90%; margin:0 auto;">
    <h2>Splashes</h2>
    <div class="graph">
        <div class="tables" style="align-items:center">

            <table class="table table-bordered">
                <thead>
                    <tr style="align-items:center">
                        <th>#</th>
                        <th>@Html.DisplayNameFor(model => model.Name)</th>
                        <th>@Html.DisplayNameFor(model => model.Enable)</th>
                        <th>@Html.DisplayNameFor(model => model.Width)</th>
                        <th>@Html.DisplayNameFor(model => model.Height)</th>
                        <th>Photo</th>
                    </tr>
                </thead>
                <tbody style="align-items:center">
                    @{
                        int counter = 0;
                    }
                    @foreach (var item in Model)
                    {
                        counter++;
                        <tr>
                            <th scope="row">@counter</th>
                            <td>@Html.ActionLink(item.Name, "Details", new { id = item.ID })</td>
                            <td>@Html.DisplayFor(modelItem => item.Enable)</td>
                            <td>@Html.DisplayFor(modelItem => item.Width)</td>
                            <td>@Html.DisplayFor(modelItem => item.Height)</td>
                            <td><img src="~/Uploads/Splashes/@(item.ID).jpg" alt="" width="60" height="60" /></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>